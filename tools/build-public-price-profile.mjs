import fs from 'fs';import zlib from 'zlib';
const src=fs.readFileSync('price-online-v041.js','utf8');
const m=src.match(/,B='([^']+)'/);if(!m)throw Error('online price blob not found');
const pairs=JSON.parse(zlib.inflateSync(Buffer.from(m[1],'base64')).toString());
fs.mkdirSync('audit-out',{recursive:true});
const js=`// ShelfCheck v0.66 — full audited public PriceCharting profile. Generated from price-online-v041.\n(()=>{const FULL=${JSON.stringify(pairs)};let tries=0,map=new Map();const apply=()=>{tries++;if(typeof byId==='undefined'||typeof norm!=='function'||typeof priceFor!=='function'){if(tries<200)setTimeout(apply,100);return;}if(!map.size){for(const [id,cents] of FULL){const x=byId.get(+id);if(!x||x.set!=='INCLUDED')continue;const m=+cents/100;map.set(norm(x.title),{t:x.title,m:+m.toFixed(2),s:+(m*.70).toFixed(2),g:+(m*.85).toFixed(2),x:+(m*1.10).toFixed(2),source:'PriceCharting audited public profile'});}}if(!window.__SHELFCHECK_FULL_PUBLIC_PRICE_WRAPPED){window.__SHELFCHECK_FULL_PUBLIC_PRICE_WRAPPED=true;const prev=priceFor;priceFor=function(x){let p=prev(x);if(p)return p;p=map.get(norm(x.title));if(p)return p;for(const a of aliasesById.get(x.id)||[]){p=map.get(a);if(p)return p}return null;};}window.SHELFCHECK_FULL_PUBLIC_PRICES={sourcePairs:FULL.length,active:map.size};if(map.size&&typeof resetBrowse==='function')resetBrowse();};apply();})();\n`;
fs.writeFileSync('audit-out/public-prices-full-v066.js',js);fs.writeFileSync('public-prices-full-v066.js',js);console.log(JSON.stringify({publicPricePairs:pairs.length,bytes:Buffer.byteLength(js)}));
